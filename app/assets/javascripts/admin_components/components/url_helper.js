const backend_activities_path = "/backend/activities";

const create_backend_activity_path = "/backend/activities";
function update_backend_activity_path(id){
  return "/backend/activities/" + id;
}
function destroy_backend_activity_path(id){
  return "/backend/activities/" + id;
}
